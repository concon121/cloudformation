          - Name: "RELEASE_NAME"
            Value: "app-source.zip"
          - Name: "S3_BUCKET"
            Value:
              Ref: <%= @appBucket %>
          - Name: "ROOT_DIRECTORY"
            Value: !Ref 'WebsiteRootDirectory'
          - Name: "CLOUDFRONT_DIST_ID"
          <% if @cloudfront && !@cloudfront.nil? %>
            Value:
                Ref: <%= @cloudfrontLogicalId %>
          <% else %>
            Value: 'N/A'
          <% end %>
