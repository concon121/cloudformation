  <%= @id %>Policy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket:
        Ref: <%= @id %>
      PolicyDocument:
        Statement:
          - Sid: 'DenyAll'
            Effect: 'Deny'
            Principal: '*'
            Action: 's3:*'
            Resource: !Join ['', ['arn:aws:s3:::', <%= @bucketName %>, '/*']]
