  <%= @id %>:
    Type: "AWS::IAM::Policy"
    Properties:
      PolicyName: <%= @name %>
      Roles:
        - !Ref UserRole
      PolicyDocument:
        Statement:
<%= partial('iam/policies/actions/codepipeline.yaml.erb',
            pipelines: ["!Join [\'-\', [!Ref \'ProjectName\', \'Pipeline\', \'@tag\']]"]) %>
