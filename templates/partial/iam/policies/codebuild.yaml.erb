  <%= @id %>:
    Type: "AWS::IAM::Policy"
    Properties:
      PolicyName: <%= @name %>
      Roles:
        - !Ref UserRole
      PolicyDocument:
        Statement:
<%= partial('iam/policies/actions/codebuild.yaml.erb', codebuilds: @codebuilds) %>
